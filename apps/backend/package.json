{
  "name": "strategic-planning-backend",
  "version": "1.0.0",
  "description": "AI-Powered Strategic Planning Platform - Backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "test:coverage": "pytest --cov=.",
    "lint": "ruff check .",
    "format": "ruff format .",
    "type-check": "mypy ."
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
